
let HD=$HOME/.config/ion/rc
if test -d $HD and -r $HD and -x $HD
  for file in @split($(find $HD -type f -name "*.ion") \n)
      if test -r $file
        source $file
      end
  end
end

if test -e $HOME/.cargo/bin/starship && contains $PATH ${HOME}/.cargo/bin
  eval $(starship init ion)
end
