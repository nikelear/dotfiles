hyperfine -w 5 -r 50 'bash -i -c exit'
zsh
#1707720912
su
#1707720942
#1707721007
code /home/sekiyas
cs
echo $(${HOME}/.local/bin/mise activate zsh)
echo $(${HOME}/.local/bin/mise activate zsh) > ~/hoge
ps -ef
cat $(mise activate zsh)
echo $(mise activate zsh)
echo "$(mise activate zsh)"
echo "$(${HOME}/.local/bin/mise activate zsh)" > ~/hoge
sudo zypper in sheldon
code ~
code /etc
ls -a
cd bash
bash
ln -snf /etc/ssl/ca-bundle.pem /etc/ssl/certs/ca-certificates.crt
sudo ln -snf /etc/ssl/ca-bundle.pem /etc/ssl/certs/ca-certificates.crt
as
ls -l /var/lib/ca-certificates/pem
ls -l /etc/ssl/
git config --list
sudo zypper in delta
ls /etc/ssl/
rm -rf zenn-community
ls -l /etc/ssl
git clone https://github.com/zenn-dev/zenn-community
deno deno run --allow-net hoge.ts
update-ca-certificates
sudo update-ca-certificates
ls -l /etc/pki/trust/anchors
deno run --allow-net hoge.ts
sudo -e /etc/zprofile
ls -l /etc/ssl/certs
test -e /etc/ssl/certs/ca-certifiates.crt
cat /etc/ssl/certs/ca-certifiates.crt
cat /var/lib/ca-certificates/ca-bundle.pem
sudo zypper in eza
eza -l --icons --git /var/lib/ca-certificates/
ln -snf /var/lib/ca-certificates/ca-bundle.pem /etc/ssl/certs/ca-certificates.crt
sudo ln -snf /var/lib/ca-certificates/ca-bundle.pem /etc/ssl/certs/ca-certificates.crt
eza -l --icons --git /etc/ssl/certs
deno run --allow-net ~/hoge.ts
sudo zypper in bat
bat /etc/profile
git commit -m 'edit zeno (when nothing deno)'
sudo zypper in nvim
vim .
nvim .
vim boke.txt
sudo zypper in neovim
mise install nodejs
mise use --global nodejs
node
mise install nodejs@latest
cd config
cd z
cd zsh
sudo zypper in fzf
cd dotfiles/config/zsh
eza -l --icons --git 
eza -l --icons --git -a
nvim .zshenv
vim .zshenv
nvim boke.ts
deno uninstall
deno --hint
deno --help
mise use --global deno@latest
which deno
git commit -m "refresh all"
vim boke.ts
git rm -r --cached coc
git --list-files
git --help
git rm -r --cached config/coc
git rm --cached config/coc
git ls-files
git commit -m "fix ignore"
git reset
git reset --soft HEAD^
git commit -m "fix edit"
git diff HEAD~1 HEAD~2
git diff @^ @^^
git commit -m 'add git snippet'
git push 
vim ~/boke.ts
echo "boke~" > boke.lua
vim boke.lua
fzf
\:q
vim config/nvim__
vim config/nvim__/init.lua
git diff
'lambdalisue/gin.vim'
nvim README.md
vim README.md
nvim
vim
git add .
git commit -m 'vim change'
git push
cd dotfiles
git reset --hard
git reset --hard HEAD
git reset --hard @^
git diff @^
git pull
git fetch && git merge
cp /mnt/c/Users/yseki.DKR/CPD1000 /home/sekiyas/CPD1000
cp -r /mnt/c/Users/yseki.DKR/CPD1000 /home/sekiyas/CPD1000
eza --icons -l
sudo zypper in ripgrep
cd CPD1000
grep
grep ,.
grep DE
grep "DE"
rg ",."
rg ",\."
rm -rf CPD1000
mise p --help
mise p a ghq
mise i ghq latest
mise i ghq
./.local/share/mise/shims/ghq
mise use --global go@latest
go install github.com/x-motemen/ghq@latest
code .
which ghq
mise --help
rustup --help
rustup self --help
rustup self uninstall
mise
mise use --global rust@latest
sudo zypper in gcc
cargo install skim
exec $SHELL -l 
command -v deno
hyperfine -w 5 -r 50 'zsh -i -c exit'
cd
printenv
: 1707896717:0;cd
: 1707896718:0;printenv
: 1707896742:0;hyperfine -w 5 -r 50 'zsh -i -c exit'
: 1707896744:0;cd
: 1707896746:0;hyperfine -w 5 -r 50 'zsh -i -c exit'
: 1707896890:0;echo $(starship init powershell) > ~/boke.txt
: 1707898136:0;cd dotfiles/
: 1707898138:0;git add .
: 1707898146:0;git commit -m "fix zsh"
: 1707898162:0;git push
: 1707953415:0;cd
: 1707953418:0;cd dotfiles/
: 1707953422:0;git pull
: 1707953443:0;git diff @^
: 1707953452:0;git diff
: 1707953460:0;code .
: 1707953900:0;sudo zypper install git-delta
: 1707953955:0;git diff @^
: 1707953961:0;git diff HEAD
: 1707953977:0;git add .
